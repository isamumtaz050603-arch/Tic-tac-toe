# Tic-tac-toe
This C++ Tic-Tac-Toe game uses a 2D array for a 3x3 board. It features dynamic gallows-free turn switches between 'X' and 'O', grid rendering, case validation, and automatic win or draw detection via explicit row, column, and diagonal tracking. It prompts coordinate inputs in a continuous loop.
